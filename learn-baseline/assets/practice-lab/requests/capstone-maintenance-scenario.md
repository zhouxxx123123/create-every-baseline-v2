# Engineering Maintenance Capstone

A user reports that Task search sometimes returns archived Tasks even when
`includeArchived` is false. No failing test or confirmed cause exists.

Build the smallest trustworthy feedback loop, separate observation from hypothesis,
and route any architecture concern without performing an unapproved rewrite. Keep
all Git operations local and non-destructive.
