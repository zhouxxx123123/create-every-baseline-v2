interface:
  display_name: "Start Setup"
  short_description: "Configure a project or update its board"
  default_prompt: "Use $start-setup for a full project setup, or use update-board mode to add, repair, or upgrade the existing project's GitHub Project and local HTML board projections without reopening its other setup choices."
policy:
  allow_implicit_invocation: false
