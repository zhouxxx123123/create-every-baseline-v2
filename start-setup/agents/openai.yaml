interface:
  display_name: "Start Setup"
  short_description: "Configure a project, product catalog, or board"
  default_prompt: "Use $start-setup for a full project setup, or use update-board mode to add, repair, or upgrade the existing project's product catalog, GitHub Project, and local HTML board projections without reopening its other setup choices."
policy:
  allow_implicit_invocation: false
