interface:
  display_name: "Start Setup"
  short_description: "Choose Git storage and configure repo workflows"
  default_prompt: "Use $start-setup to choose local-only or GitHub-backed Git and configure this repository's workflow skills."
policy:
  allow_implicit_invocation: false
