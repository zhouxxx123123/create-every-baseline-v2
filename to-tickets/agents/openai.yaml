interface:
  display_name: "To Tickets"
  short_description: "Validate and publish tracer-bullet tickets"
  default_prompt: "Use $to-tickets to split this approved specification into validated vertical tickets and publish them after I confirm the plan."
policy:
  allow_implicit_invocation: false
