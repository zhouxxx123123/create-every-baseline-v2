interface:
  display_name: "Handoff"
  short_description: "Compact a conversation into a handoff"
  default_prompt: "Use $handoff to prepare this conversation for the next session and preserve its return target."
policy:
  allow_implicit_invocation: false
